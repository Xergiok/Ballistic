clear; close all; clc;
format short;

% opts = struct('WindowStyle','non-modal',... 
%               'Interpreter','tex');
% f = warndlg('\color{blue} Pressing ACCEPT clears memory',...
%              'Memory Warning', opts);

% opts.Interpreter = 'tex';
% % Include the desired Default answer
% opts.Default = 'ok';
% % Use the TeX interpreter to format the question
% quest = {   'afasdf', ...
%             'asdgsd'    };
% answer = questdlg(quest,'Инфо',...
%                   'ok',opts);

a = zeros(4, 1)
b = ones(3, 1)
size(a , 2)